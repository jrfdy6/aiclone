# Persistent State

Live runtime updates now write to `memory/runtime/persistent_state.md`.

Read through the canonical logical path (`memory/persistent_state.md`) using the snapshot/runtime resolver so operators and automation can consume the latest state without dirtying the tracked worktree.
