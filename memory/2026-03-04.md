## Backend + collectors
- Added env loader + Firestore/local store scaffolding (knowledge/playbooks/prospects) and seeded Firestore with 106 docs.
- Configured secrets/railway.env + ~/.netrc so this workspace can push to github.com/jrfdy6/aiclone via PAT.
- Deployed new FastAPI routes to Railway (after fixing path guards + prefix issues); `/api/knowledge` and `/api/analytics` now live for the upcoming Discord/Railway collectors.
- Priority updated: finish Discord compliance dashboard + Railway health collectors and Gmail/Calendar command flows before touching prospect ingestion or long-tail integrations (LinkedIn/Firecrawl/Perplexity).
- After collectors + Gmail/Calendar loops are stable, next priority is reshaping the Railway frontend UI before expanding into a multi-agent system.
