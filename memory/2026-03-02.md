# 2026-03-02
- Feeze set operating guardrails: no autonomous executions, deployments, merges, or public content without explicit approval; avoid short-term wins that undermine long-term leverage; always surface second-order effects, ethical tradeoffs, brand durability concerns, and infrastructure risk.
- Treat production-impacting changes (auth, payments, data schemas, infra configs, env vars) with the highest scrutiny; default to systems thinking over tactical hacks; remain internal-facing and avoid public/general channels unless explicitly summoned; no monitoring unrelated channels.
- First strategic objective: design and deploy a public-facing utility tool that acts as Feeze’s AI clone to help them build.
- Phase 1 interface decision: Discord chat surface will be the primary command/oversight layer; CLI support can follow for deeper infra operations.
- Clone requirements captured: focus on strategic tradeoff modeling, systems architecture design, code drafting (no auto merge), CI/CD workflow generation, deployment readiness evaluation, risk classification before deploy, and structured backlog prioritization; explicitly out of scope for phase 1 are autonomous execution, public engagement, payment systems, and multi-environment orchestration. Knowledge comes from a single GitHub repo (including CI workflows, deployment configs, guardrail/constitution files) with phased memory expansion. Guardrails include surfacing second-order effects, defaulting to speed unless high-risk triggers fire, escalation and explicit approval for auth/payment/schema/infra changes, measured executive tone, and strict bans on autodeploy, autopublish, storing master infra keys, or operating outside designated channels. Oversight loop: draft → diff analysis → risk classification → deployment readiness report → manual approval → CI trigger → deployment → post-deploy report. Security model: project-scoped tokens only, secrets kept out of Discord, rollback plan required before first production deploy; acceptable failure mode is credential rotation, unacceptable is production data exposure.
- Implementation inputs: create/anchor repo `neo-core` (holding orchestration logic, CI/CD intelligence, risk classification, deployment evaluation, infra adapters for Railway/Vercel) as single repo for phase 1. Runtime: Node.js/TypeScript deployed on Railway (phase 1) with Vercel reserved for later frontend surfaces. CI stack: GitHub Actions driving Railway deployments via decision-gated workflow (feature branch push → PR → Neo diff/risk/readiness report → manual /approve-deploy → Actions run → Railway deploy → post-deploy report). Security baseline: repo-scoped GitHub token, Railway project-scoped token, no account-level keys, no production domain automation, rollback via tagged release.
- Additional constraints: deliverables order is fixed (architecture brief → build plan/backlog → Discord command schema); architecture brief must include a drift-detection layer for guardrail monitoring/escalation. Public utility branding must be minimal, infrastructure-toned (one- or two-word names, no hype terms, no emojis, avoid using "Neo"), signaling structured thinking/decision quality. Discord command schema limited to ≤7 core commands, each mapping to a capability with structured output to keep it a control panel, not chat sprawl. Pending choice: whether `neo-core` includes a minimal internal dashboard for logs/deploy history in addition to orchestration.
- Architecture additions: dashboard must expose an immutable append-only audit log (approvals, risk calls, guardrail escalations, drift violations, rollback events); enforce separation between risk evaluation engine, approval gate, and execution adapter; define explicit failure behaviors for Discord API outages, GitHub rate limits, Railway partial deploys, and risk-engine crashes. Dashboard access = Railway-protected surface only (single operator, no custom auth) and serves observability only. Drift detection requires independent pipeline + escalation paths. Deliverable #1 must reflect all of the above with no scope creep.
- Architecture invariants finalized: audit layer must be write-once with monotonic IDs/hash chaining and structured schema (event_type, actor, input_hash, risk_score, decision, execution_ref, timestamp), rollbacks logged as new events. Component boundaries contractual—pure risk evaluation, explicit approval gate requiring operator confirmation, execution adapter that only runs with a signed approval artifact (Option B). Failure-mode matrix required for Discord outages, GitHub rate limits, Railway partial deploys, and risk engine crashes (first 5s/5m behaviors). Drift detection must define measurable signals, detection mechanisms, and escalation levels. Dashboard is read-only observability (log viewer + guardrail panels) behind Railway auth. Approval artifacts are signed entries stored in the immutable log, gating execution.
