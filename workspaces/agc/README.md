# AGC Workspace

This is the local execution lane for `agc`.

Canonical workspace key:
- `agc`

Minimum lane structure:
- `dispatch/` for Jean-Claude SOP work orders
- `briefings/` for workspace-local markdown briefings
- `docs/` for local operating docs
- `memory/` for execution-local memory and logs
- `agent-ledgers/` for structured runner traces
