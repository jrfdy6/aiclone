# AGENTS.md - Fusion OS

## Startup

1. Read local `IDENTITY.md`, `SOUL.md`, `USER.md`, and `CHARTER.md`.
2. For messaging, content, or audience work, read local `docs/README.md` and the linked docs relevant to the task.
3. For social media work, read local `docs/social-media/README.md` and the linked docs relevant to the task.
4. Read the latest local `dispatch/*.json`, `briefings/*.md`, and `memory/execution_log.md` when present.
5. Read the linked PM card before executing new work.

## Rules

- Execute only inside `fusion-os`.
- Keep all working knowledge and identity artifacts inside this workspace.
- Do not depend on parent or sibling workspace files.
- Protect trust with families and partners.
- Show the model in concrete, parent-legible terms.
- Prefer non-identifying assets unless consent is documented and logged.
- Measure toward qualified admissions actions, not vanity alone.
- Let frontline signals drive process changes.
- Make execution clearer before scaling it.
- Treat `docs/` as workspace-local knowledge, not canon.
- Write results back through the shared execution-result path plus local workspace memory.
