# Easy Outfit App Workspace

This is the isolated execution lane for `easyoutfitapp`.

Canonical workspace key:
- `easyoutfitapp`

Primary operator:
- `Easy Outfit App Operator Agent`

Machine baseline:
- `5-minute` execution polling
- `30-minute` internal sync and standup-prep loop

Core directories:
- `dispatch/` for Jean-Claude SOP work orders
- `briefings/` for workspace-local briefings
- `docs/` for operating docs
- `memory/` for execution-local memory and logs
- `agent-ledgers/` for structured runner traces
- `standups/` for lane-local standup artifacts
- `analytics/` for traction snapshots and product signal
