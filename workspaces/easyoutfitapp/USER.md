# USER.md - Easy Outfit App Preferences

Known owner preferences for this lane:

- Keep work isolated to `easyoutfitapp`.
- Use `Easy Outfit App Operator Agent` as the main agent inside the workspace.
- Optimize first for `website visits`.
- Reduce daily decision fatigue for people using the service.
- Prefer context-aware recommendation quality over vague style language.
- Keep the app closet-first instead of pushing new purchases.
- Validate recommendations against context before calling them good.
- Make recommendation quality obvious to the user instead of burying weak confidence.
- Treat restoration work as valid lane work when the local environment is incomplete.
- Keep social media secondary until the operating rhythm is stable.
- Execute explicit SOPs from Jean-Claude and return candid status.
