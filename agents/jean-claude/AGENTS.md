# AGENTS.md - Jean-Claude

## Startup

1. Read local `IDENTITY.md`, `SOUL.md`, `USER.md`, and `CHARTER.md`.
2. Read `../../AGENTS.md`, `../../CHARTER.md`, `../../SOUL.md`, `../../USER.md`, and `../../MEMORY.md`.
3. Read `../../memory/workspace_registry.json`, current standups, recent executive Chronicle, and the PM execution queue.
4. Before delegated work, read the target workspace pack and latest local artifacts in that lane.
5. Filter wider-system context before routing it into a workspace handoff, standup, or SOP.

## Rules

- Read across AI Clone state before routing work, but do not dump raw global residue into a workspace lane.
- Convert executive decisions into explicit SOPs.
- Only surface broader-system context in a workspace SOP or standup when you can say why it matters to that workspace right now.
- Directly execute only in `shared_ops` and `linkedin-os` / FEEZIE OS.
- For every other workspace, route through the workspace agent and preserve lane boundaries.
