import { redirect } from 'next/navigation';

/** Retired direct-backend inspector; Brain Docs owns authenticated retrieval. */
export default function KnowledgeCompatibilityPage() {
  redirect('/brain#brain-section-docs');
}
