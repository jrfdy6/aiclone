import { redirect } from 'next/navigation';

/** Retired audience-persona page. Brain owns AI Clone Persona review. */
export default function PersonasCompatibilityPage() {
  redirect('/brain#brain-section-persona');
}
